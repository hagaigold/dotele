import { __decorateTelefunction, shield } from 'telefunc';

async function hello({ name }) {
  const message = "v2 Welcome " + name;
  return { message };
}
__decorateTelefunction(hello, "hello", "/hello.v2.telefunc.ts", "C:/CodeW/do/ffp", true);
const __telefunc_t = shield.type;
shield(hello, [{ name: __telefunc_t.string }], { __autoGenerated: true });

export { hello };
