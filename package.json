{
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr",
    "build:client": "vite build",
    "build:digitalocean": "npm ci && npm run build:client",
    "preview": "npm run build && vite preview",
    "test_pre": "vite preview"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "telefunc": "0.1.60",
    "vite": "^4.3.9"
  },
  "type": "module",
  "devDependencies": {
    "express": "^4.18.2"
  }
}
